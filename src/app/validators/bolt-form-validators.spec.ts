import { BoltFormValidators } from './bolt-form-validators';

describe('BoltFormValidators', () => {
  it('should create an instance', () => {
    expect(new BoltFormValidators()).toBeTruthy();
  });
});
